from setuptools import setup, find_packages

setup(
    name="temporal_spatial_memory",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add any dependencies here
    ],
    author="Your Name",
    author_email="your.email@example.com",
    description="A Temporal-Spatial Memory Database",
    keywords="memory, database, temporal, spatial",
    python_requires=">=3.6",
) 